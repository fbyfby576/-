# 手机随时访问方案

这个项目是纯静态网页，最适合放到静态托管平台。发布后会得到一个 HTTPS 网址，手机不需要和电脑在同一个网络，也不需要电脑开着。

## 推荐路线

### 方案 A：Netlify Drop，最快

适合先自己用起来。

1. 把整个项目文件夹压缩成 zip，或直接准备这个文件夹。
2. 打开 Netlify Drop：https://app.netlify.com/drop/
3. 拖入文件夹或 zip。
4. 发布完成后，用手机打开它给你的网址。
5. 在手机浏览器里选择“添加到主屏幕”。

以后更新菜单时，重新拖一次新的项目文件夹或 zip。

### 方案 B：GitHub Pages，适合长期维护

适合每次改代码后自动更新。

1. 在 GitHub 创建一个仓库。
2. 上传这个项目目录里的所有文件。
3. 在仓库设置里打开 Pages。
4. Source 选择主分支。
5. 发布后用手机打开 GitHub Pages 网址。
6. 在手机浏览器里选择“添加到主屏幕”。

### 方案 C：Cloudflare Pages，适合以后加域名

适合想绑定自己的域名，或者后续继续扩展。

1. 打开 Cloudflare Pages。
2. 选择上传静态文件，或连接 GitHub 仓库。
3. 构建命令留空。
4. 输出目录选择项目根目录。
5. 发布后用手机打开它给你的网址。

## PWA 支持

项目已经加入：

- `manifest.webmanifest`：告诉手机这是一个可安装网页应用。
- `sw.js`：缓存核心页面和样式，方便离线打开。
- `pwa.js`：在 HTTPS 网站上自动注册离线缓存。
- `icon.svg`：主屏幕图标。

注意：PWA 离线缓存需要 HTTPS，所以上传到 Netlify、GitHub Pages、Cloudflare Pages 后才会完整生效。本地双击 HTML 时，页面仍能打开，但不会注册离线缓存。

## 数据说明

现在的完成度记录存在手机本地浏览器的 `localStorage` 里：

- 同一台手机、同一个浏览器会保留记录。
- 换手机、换浏览器不会自动同步。
- 如果清理浏览器网站数据，记录会消失。

如果以后需要多设备同步，就需要再加一个后端或云数据库。
